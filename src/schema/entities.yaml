---
# Ordering and attributes of entities
- sub:
    format: label
- ses:
    format: label
- task:
    format: label
- acq:
    format: label
- ce:
    format: label
- rec:
    format: label
- dir:
    format: label
- run:
    format: index
- mod:
    format: label
- echo:
    format: index
- recording:
    format: label
- proc:
    format: label
- space:
    format: label
- split:
    format: index

---
- datatypes:
    - meg
    - eeg
    - ieeg
  suffixes:
    - events
  extensions:
    - .json
    - .tsv
  entities:
    sub: required
    ses: optional
    task: required
    acq: optional
    run: optional

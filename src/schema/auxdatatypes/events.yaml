---
- datatypes:
    - meg
    - eeg
    - ieeg
  suffixes:
    - events
  extensions:
    - .json
    - .tsv
  entities:
    subject: required
    session: optional
    task: required
    run: optional

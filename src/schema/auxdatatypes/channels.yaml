---
- datatypes:
    - meg
    - eeg
    - ieeg
  suffixes:
    - channels
  extensions:
    - .json
    - .tsv
  entities:
    sub: required
    ses: optional
    task: required
    acq: optional
    run: optional

---
- datatypes:
    - meg
    - eeg
    - ieeg
  suffixes:
    - channels
  extensions:
    - .json
    - .tsv
  entities:
    subject: required
    session: optional
    task: required
    run: optional

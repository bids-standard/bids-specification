---
name: ScreenDistance
description: |
  Screen distance in cm
  (for example `60` for a screen distance of 60 cm),
  if no screen use `n/a`.
anyOf:
  - type: number
  - type: string
    enum:
      - n/a

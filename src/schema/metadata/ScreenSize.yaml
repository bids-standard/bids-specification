---
name: ScreenSize
description: |
  Screen size in cm
  (for example `[47.2, 29.5]` for a screen of 47.2-length by 29.5-height cm),
  if no screen use `n/a`.
anyOf:
  - type: array
    items:
      type: number
  - type: string
    enum:
      - n/a

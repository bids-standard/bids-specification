---
# Files and directories that can be found at any level of a dataset.

# Provenance json files
provenance:
  level: optional
  suffixes:
    - act
    - base
    - ent
    - env
    - soft
  extensions:
    - .json
  entities:
    subject: optional
    session: optional
    prov: required

# Provenance ld files
provenanceld:
  level: optional
  suffixes:
    - all
  extensions:
    - .jsonld
  entities:
    subject: optional
    session: optional
    prov: required

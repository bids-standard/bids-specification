---
svs:
  suffixes:
    - svs
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - mrs
  entities:
    subject: required
    session: optional
    task: optional
    acquisition: optional
    reconstruction: optional
    run: optional
    echo: optional
    inversion: optional
    nucleus: optional
    volume: optional

mrsi:
  suffixes:
    - mrsi
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - mrs
  entities:
    subject: required
    session: optional
    task: optional
    acquisition: optional
    reconstruction: optional
    run: optional
    echo: optional
    inversion: optional
    nucleus: optional
    volume: optional

unlocalized:
  suffixes:
    - unloc
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - mrs
  entities:
    subject: required
    session: optional
    task: optional
    acquisition: optional
    reconstruction: optional
    run: optional
    echo: optional
    inversion: optional
    nucleus: optional
    volume: optional

reference:
  suffixes:
    - ref
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - mrs
  entities:
    subject: required
    session: optional
    task: optional
    acquisition: optional
    reconstruction: optional
    run: optional
    echo: optional
    inversion: optional
    nucleus: optional
    volume: optional

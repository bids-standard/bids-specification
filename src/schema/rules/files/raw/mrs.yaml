---
mrs:
  suffixes:
    - svs
    - mrsi
    - unloc
    - mrsref
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - mrs
  entities:
    $ref: meta.templates.raw.recording.entities
    task: optional
    reconstruction: optional
    echo: optional
    inversion: optional
    nucleus: optional
    volume: optional

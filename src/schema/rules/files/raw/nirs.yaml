---
nirs:
  suffixes:
    - nirs
  extensions:
    - .snirf
    - .json
  datatypes:
    - nirs
  entities:
    $ref: rules.files.raw.common.baseline.entities
    task: required
    acquisition: optional
    run: optional

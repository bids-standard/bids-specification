---
nirs:
  suffixes:
    - nirs
  extensions:
    - .snirf
    - .json
  datatypes:
    - nirs
  entities:
    $ref: meta.templates.raw.base.entities
    task: required
    acquisition: optional
    run: optional

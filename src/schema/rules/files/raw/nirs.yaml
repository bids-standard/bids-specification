---
nirs:
  suffixes:
    - nirs
  extensions:
    - .snirf
    - .json
  datatypes:
    - nirs
  entities:
    $ref: meta.templates.raw_base_entities
    task: required
    acquisition: optional
    run: optional

---
# Non-continuous data
noncontinuous:
  $ref: meta.templates.raw.task
  suffixes:
    - beh
  extensions:
    - .tsv
    - .json
  datatypes:
    - beh
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

# Audio recordings
audio:
  suffixes:
    - audio
  extensions:
    - .flac
    - .mp3
    - .ogg
    - .wav
    - .json
  datatypes:
    - beh
  entities:
    subject: required
    session: optional
    task: optional
    acquisition: optional
    run: optional
    recording: optional
    split: optional

# Video recordings
video:
  suffixes:
    - video
  extensions:
    - .mp4
    - .mkv
    - .avi
    - .json
  datatypes:
    - beh
  entities:
    subject: required
    session: optional
    task: optional
    acquisition: optional
    run: optional
    recording: optional
    split: optional

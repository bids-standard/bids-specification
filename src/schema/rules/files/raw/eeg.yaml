---
eeg:
  suffixes:
    - eeg
  extensions:
    - .json
    - .edf
    - .vhdr
    - .vmrk
    - .eeg
    - .set
    - .fdt
    - .bdf
  datatypes:
    - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

events:
  suffixes:
    - events
  extensions:
    - .json
    - .tsv
  datatypes:
    - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

photo:
  suffixes:
    - photo
  extensions:
    - .jpg
    - .png
    - .tif
  datatypes:
    - eeg
  entities:
    subject: required
    session: optional
    acquisition: optional

timeseries:
  suffixes:
    - physio
    - stim
  extensions:
    - .tsv.gz
    - .json
  datatypes:
    - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    recording: optional

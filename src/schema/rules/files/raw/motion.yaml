---
motion:
  suffixes:
    - motion
  extensions:
    - .tsv
    - .json
  datatypes:
    - motion
  entities:
    $ref: meta.templates.raw_base_entities
    task: required
    tracksys: required
    acquisition: optional
    run: optional

---
motion:
  suffixes:
    - motion
  extensions:
    - .tsv
    - .json
  datatypes:
    - motion
  entities:
    $ref: rules.files.raw.common.baseline.entities
    task: required
    tracksys: required
    acquisition: optional
    run: optional

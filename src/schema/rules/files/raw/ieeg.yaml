---
ieeg:
  suffixes:
    - ieeg
  extensions:
    - .json
    # .vhdr, .vmrk, .eeg must work as triplet
    - .vhdr
    - .vmrk
    - .eeg
    # .set file with an OPTIONAL .fdt
    - .set
    - .fdt
    - [.nwb, .edf, .mefd/]
  datatypes:
    - ieeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

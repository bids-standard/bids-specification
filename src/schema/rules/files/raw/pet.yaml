---
pet:
  selectors:
    #- '!("tpl" in entities)'
  suffixes:
    - pet
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - pet
  entities:
    $ref: meta.templates.raw.pet.entities

blood:
  selectors:
    #- '!("tpl" in entities)'
  suffixes:
    - blood
  extensions:
    - .tsv
    - .json
  datatypes:
    - pet
  entities:
    $ref: meta.templates.raw.pet.entities
    recording: required

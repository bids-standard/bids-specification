---
pet:
  suffixes:
    - pet
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - pet
  entities:
    $ref: meta.templates.raw_base_entities
    task: optional
    tracer: optional
    reconstruction: optional
    run: optional

blood:
  suffixes:
    - blood
  extensions:
    - .tsv
    - .json
  datatypes:
    - pet
  entities:
    $ref: meta.templates.raw_base_entities
    task: optional
    tracer: optional
    reconstruction: optional
    run: optional
    recording: required

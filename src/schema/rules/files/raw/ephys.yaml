---
cephys:
  suffixes:
    - ecephys
    - icephys
  extensions:
    - .nwb
    # possible future: serialization in .zarr format to accompany .ome.zarr
    # - .nwb.zarr
    - .nix
  datatypes:
    - cephys
  entities:
    subject: required
    session: optional
    sample: optional
    task: optional
    acquisition: optional
    run: optional
    split: optional

probes:
  suffixes:
    - probes
  extensions:
    - .tsv
  datatypes:
    - cephys
  entities:
    subject: required
    session: optional
    sample: optional
    task: optional
    acquisition: optional
    run: optional
    split: optional

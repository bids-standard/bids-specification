---
ephys:
  suffixes:
    - ephys
  extensions:
    - .nwb
    # possible future: serialization in .zarr format to accompany .ome.zarr
    # - .nwb.zarr
    - .nix
  datatypes:
    - ephys
  entities:
    subject: required
    session: optional
    sample: optional
    task: optional
    acquisition: optional
    run: optional
    split: optional

probes:
  suffixes:
    - probes
  extensions:
    - .tsv
  datatypes:
    - ephys
  entities:
    subject: required
    session: optional
    sample: optional
    task: optional
    acquisition: optional
    run: optional
    split: optional

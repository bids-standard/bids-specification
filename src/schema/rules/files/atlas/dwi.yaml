---
dwi:
  suffixes:
    - dwi
  extensions:
    - .nii.gz
    - .nii
    - .json
    - .bvec
    - .bval
  datatypes:
    - dwi
  entities:
    $ref: meta.templates.atlas_base_entities
    acquisition: optional
    reconstruction: optional
    direction: optional
    run: optional
    part: optional
    chunk: optional

sbref:
  suffixes:
    - sbref
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - dwi
  entities:
    $ref: meta.templates.atlas_base_entities
    acquisition: optional
    reconstruction: optional
    direction: optional
    run: optional
    part: optional
    chunk: optional

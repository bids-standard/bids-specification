---
func:
  suffixes:
    - bold
    - cbv
    - sbref
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - func
  entities:
    $ref: rules.files.atlas.common.baseline.entities
    task: required
    acquisition: optional
    ceagent: optional
    reconstruction: optional
    direction: optional
    run: optional
    echo: optional
    part: optional
    chunk: optional

phase:
  suffixes:
    - phase # deprecated
  extensions:
    - .nii.gz
    - .nii
    - .json
  datatypes:
    - func
  entities:
    $ref: rules.files.atlas.common.baseline.entities
    task: required
    acquisition: optional
    ceagent: optional
    reconstruction: optional
    direction: optional
    run: optional
    echo: optional
    chunk: optional

# Rules for phenotype files
---
# 51
PhenotypeSubjectsMissing:
  issue:
    code: PHENOTYPE_SUBJECTS_MISSING
    message: |
      A phenotype/ .tsv file lists subjects that were not found in
      the participant_id column found in the participants.tsv file.
    level: error
  selectors:
    - datatype == 'phenotype'
    - extension == '.tsv'
    - type(dataset.subjects.participant_id) != 'null'
  checks:
    # A∩B == A iff A⊆B
    - |
      length(intersects(unique(columns.participant_id), dataset.subjects.participant_id))
      == length(unique(columns.participant_id))

PhenotypeDemographicsFileRequiredByTool:
  issue:
    code: PHENOTYPE_DEMOGRAPHICS_FILE_REQUIRED
    message: |
      A phenotype TSV file contains a session_id column but no
      /phenotype/tool-demographics_phenotype.tsv file
      was found in the /phenotype/ directory.
    level: error
  selectors:
    - datatype == 'phenotype'
    - suffix == 'phenotype'
    - extension == '.tsv'
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
    - type(columns.session_id) != 'null'
  checks:
    - exists('phenotype/tool-demographics_phenotype.tsv', 'dataset')

PhenotypeDemographicsFileRequiredBySessionDir:
  issue:
    code: PHENOTYPE_DEMOGRAPHICS_FILE_REQUIRED
    message: |
      A phenotype/tool-Demographics_phenotype.tsv file is required when session
      directories are present.
    level: error
  selectors:
    - path == '/dataset_description.json'
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
    - length(glob("sub-*/ses-*")) > 0
  checks:
    - exists('phenotype/tool-demographics_phenotype.tsv', 'dataset')

PhenotypeSessionLevels:
  issue:
    code: PHENOTYPE_SESSION_LEVELS
    message: |
      A phenotype tsv sidecar is missing the `Levels` definition for its `session_id` object.
    level: error
  selectors:
    - entities.tool == demographics
    - suffix == 'phenotype'
    - extension == '.tsv'
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
    - type(sidecar.session_id) != 'null'
  checks:
    - type(sidecar.session_id.Levels) != 'null'

PhenotypeSessionDirectoryMissing:
  issue:
    code: SESSION_REQUIRED
    message: |
      No subject session directories were found. Session directories are required for all subjects when `session_id`
      columns are used in any phenotype tsv file.
    level: error
  selectors:
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
    - entities.tool == 'Demographics'
    - suffix == 'phenotype'
    - extension == '.tsv'
    - type(columns.session_id) != 'null'
    - length(glob("sub-*")) > 0
  checks:
    - length(glob("sub-*/ses-*")) > 0

PhenotypeSessionRequired:
  issue:
    code: SESSION_REQUIRED
    message: |
      A subjects data file is missing the `ses` entity. Every data file must be in a session directory and use the ses
      entity when any other multi-session data is used in the dataset.
    level: error
  selectors:
    - type(entities.sub) != 'null'
    - "!intersects([extension], ['.tsv', '.json'])"
    - length(glob("sub-*/ses-*")) > 0
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
  checks:
    - type(entities.ses) != 'null'

PhenotypeIndexColumnsRequired:
  issue:
    code: PHENOTYPE_TOOL_MISSING_INDEX_COLUMNS
    message: |
      A phenotype file is missing the `IndexColumns` key and value in its associated sidecar.
    level: error
  selectors:
    - suffix == 'phenotype'
    - extension == '.tsv'
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
    - type(columns.session_id) != 'null'
  checks:
    - type(sidecar.IndexColumns) != 'null'

PhenotypeSessionColumnExists:
  issue:
    code: PHENOTYPE_SESSION_LEVELS
    message: |
      /phenotype/tool-Demographics_phenotype.tsv missing `Levels` definition for `session_id` object in
      its sidecar.
    level: error
  selectors:
    - suffix == 'phenotype'
    - extension == '.tsv'
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
    - length(glob("sub-*/ses-*")) > 0
  checks:
    - type(sidecar.session_id) != 'null'
    - type(sidecar.session_id.Levels) != 'null'

ParicipantsSidecarRequired:
  issue:
    code: wat
    message: wat
  selectors:
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
    - path == '/participants.tsv'
  checks:
    - type(sidecar.participant_id) != 'null'

PhenotypeSidecarRequired:
  issue:
    code: PHENOTYPE_SESSION_LEVELS
    message: |
      Phenotype tsvs are required to have a sidecar with a MeasurementToolMetadata object with either a `TermURL` or
      `Description` key defined.
    level: error
  selectors:
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
    - suffix == 'phenotype'
    - extension == '.tsv'
    - type(entities.tool) != 'null'
  checks:
    - type(sidecar.MeasurementToolMetadata) != 'null'
    - type(sidecar.MeasurementToolMetadata.TermURL) != 'null'
      || type(sidecar.MeasurementToolMetadata.Description) != 'null'

PhenotypeDemographicsFileRequiredBySessionDir:
  issue:
    code: PHENOTYPE_DEMOGRAPHICS_MISSING_DATA
    message: |
      /phenotype/tool-Demographics_phenotype.tsv is required to have a `session_id` column defined as well as an
      `IndexColumns` defined in its sidecar session directories are present.
    level: error
  selectors:
    - path == '/phenotype/tool-Demographics_phenotype.tsv'
    - intersects(dataset.dataset_description.AdditionalValidation, ["Phenotype"])
    - length(glob("sub-*/ses-*")) > 0
  checks:
    - type(columns.session_id) != 'null'
    - type(sidecar.IndexColumns) != 'null'

---
func:
  suffixes:
  - bold
  - cbv
  - sbref
  extensions:
  - niigz
  - nii
  - json
  datatypes:
  - func
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    ceagent: optional
    reconstruction: optional
    direction: optional
    run: optional
    echo: optional
    part: optional

phase:
  suffixes:
  - phase  # deprecated
  extensions:
  - niigz
  - nii
  - json
  datatypes:
  - func
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    ceagent: optional
    reconstruction: optional
    direction: optional
    run: optional
    echo: optional

events:
  suffixes:
  - events
  extensions:
  - tsv
  - json
  datatypes:
  - func
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    ceagent: optional
    reconstruction: optional
    direction: optional
    run: optional

timeseries:
  suffixes:
  - physio
  - stim
  extensions:
  - tsvgz
  - json
  datatypes:
  - func
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    ceagent: optional
    reconstruction: optional
    direction: optional
    run: optional
    recording: optional

---
- suffixes:
  - nirs
  extensions:
  - .snirf
  - .json
  entities:
    subject: required
    session: optional
    task: required
    run: optional

- suffixes:
  - channels
  extensions:
  - .tsv
  entities:
    subject: required
    session: optional
    task: required
    run: optional

- suffixes:
  - coordsystem
  extensions:
  - .json
  entities:
    subject: required
    session: optional

- suffixes:
  - optodes
  extensions:
  - .tsv
  entities:
    subject: required
    session: optional

- suffixes:
  - events
  extensions:
  - .tsv
  - .json
  entities:
    subject: required
    session: optional
    task: required
    run: optional

# Timeseries
- suffixes:
  - physio
  - stim
  extensions:
  - .tsv.gz
  - .json
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    recording: optional

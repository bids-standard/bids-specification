---
nirs:
  suffixes:
  - nirs
  extensions:
  - .snirf
  - .json
  datatypes:
  - nirs
  entities:
    subject: required
    session: optional
    task: required
    run: optional

channels:
  suffixes:
  - channels
  extensions:
  - .tsv
  datatypes:
  - nirs
  entities:
    subject: required
    session: optional
    task: required
    run: optional

coordsystem:
  suffixes:
  - coordsystem
  extensions:
  - .json
  datatypes:
  - nirs
  entities:
    subject: required
    session: optional

optodes:
  suffixes:
  - optodes
  extensions:
  - .tsv
  datatypes:
  - nirs
  entities:
    subject: required
    session: optional

events:
  suffixes:
  - events
  extensions:
  - .tsv
  - .json
  datatypes:
  - nirs
  entities:
    subject: required
    session: optional
    task: required
    run: optional

timeseries:
  suffixes:
  - physio
  - stim
  extensions:
  - .tsv.gz
  - .json
  datatypes:
  - nirs
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    recording: optional

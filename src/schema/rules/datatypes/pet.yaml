---
pet:
  suffixes:
  - pet
  extensions:
  - .nii.gz
  - .nii
  - .json
  datatypes:
  - pet
  entities:
    subject: required
    session: optional
    task: optional
    tracer: optional
    reconstruction: optional
    run: optional

blood:
  suffixes:
  - blood
  extensions:
  - .tsv
  - .json
  datatypes:
  - pet
  entities:
    subject: required
    session: optional
    task: optional
    tracer: optional
    reconstruction: optional
    run: optional
    recording: required

events:
  suffixes:
  - events
  extensions:
  - .tsv
  - .json
  datatypes:
  - pet
  entities:
    subject: required
    session: optional
    task: required
    tracer: optional
    reconstruction: optional
    run: optional

timeseries:
  suffixes:
  - physio
  - stim
  extensions:
  - .tsv.gz
  - .json
  datatypes:
  - pet
  entities:
    subject: required
    session: optional
    task: optional
    tracer: optional
    reconstruction: optional
    run: optional
    recording: optional

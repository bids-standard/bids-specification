---
- suffixes:
  - ieeg
  extensions:
  - .mefd/
  - .json
  - .edf
  - .vhdr
  - .eeg
  - .vmrk
  - .set
  - .fdt
  - .nwb
  datatypes:
  - ieeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

- suffixes:
  - channels
  extensions:
  - .json
  - .tsv
  datatypes:
  - ieeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

- suffixes:
  - coordsystem
  extensions:
  - .json
  datatypes:
  - ieeg
  entities:
    subject: required
    session: optional
    acquisition: optional
    space: optional

- suffixes:
  - electrodes
  extensions:
  - .json
  - .tsv
  datatypes:
  - ieeg
  entities:
    subject: required
    session: optional
    acquisition: optional
    space: optional

- suffixes:
  - events
  extensions:
  - .json
  - .tsv
  datatypes:
  - ieeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

- suffixes:
  - photo
  extensions:
  - .jpg
  entities:
    subject: required
    session: optional
    acquisition: optional

# Timeseries
- suffixes:
  - physio
  - stim
  extensions:
  - .tsv.gz
  - .json
  datatypes:
  - ieeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    recording: optional

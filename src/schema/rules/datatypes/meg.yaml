---
meg:
  suffixes:
  - meg
  extensions:
  - /  # corresponds to BTi/4D data
  - .ds/
  - .json
  - .fif
  - .sqd
  - .con
  - .raw
  - .ave
  - .mrk
  - .kdf
  - .mhd
  - .trg
  - .chn
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    processing: optional
    split: optional

calibration:
  suffixes:
  - meg
  extensions:
  - .dat
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    acquisition:
      requirement: required
      type: string
      enum:
      - calibration

crosstalk:
  suffixes:
  - meg
  extensions:
  - .fif
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    acquisition:
      requirement: required
      type: string
      enum:
      - crosstalk

headshape:
  suffixes:
  - headshape
  extensions:
  - .*
  - .pos
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    acquisition: optional

markers:
  suffixes:
  - markers
  extensions:
  - .sqd
  - .mrk
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    task: optional
    acquisition: optional
    space: optional

coordsystem:
  suffixes:
  - coordsystem
  extensions:
  - .json
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    acquisition: optional

channels:
  suffixes:
  - channels
  extensions:
  - .json
  - .tsv
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    processing: optional

events:
  suffixes:
  - events
  extensions:
  - .json
  - .tsv
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

photo:
  suffixes:
  - photo
  extensions:
  - .jpg
  - .png
  - .tif
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    acquisition: optional

timeseries:
  suffixes:
  - physio
  - stim
  extensions:
  - .tsv.gz
  - .json
  datatypes:
  - meg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    processing: optional
    recording: optional

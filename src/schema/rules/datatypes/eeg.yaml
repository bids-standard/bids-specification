---
- suffixes:
  - eeg
  extensions:
  - .json
  - .edf
  - .vhdr
  - .vmrk
  - .eeg
  - .set
  - .fdt
  - .bdf
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

- suffixes:
  - channels
  extensions:
  - .json
  - .tsv
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

- suffixes:
  - coordsystem
  extensions:
  - .json
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    acquisition: optional
    space: optional

- suffixes:
  - electrodes
  extensions:
  - .json
  - .tsv
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    acquisition: optional
    space: optional

- suffixes:
  - events
  extensions:
  - .json
  - .tsv
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

- suffixes:
  - photo
  extensions:
  - .jpg
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    acquisition: optional

# Timeseries
- suffixes:
  - physio
  - stim
  extensions:
  - .tsv.gz
  - .json
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    recording: optional

---
eeg:
  suffixes:
  - eeg
  extensions:
  - .json
  - .edf
  - .vhdr
  - .vmrk
  - .eeg
  - .set
  - .fdt
  - .bdf
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

channels:
  suffixes:
  - channels
  extensions:
  - .json
  - .tsv
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

coordsystem:
  suffixes:
  - coordsystem
  extensions:
  - .json
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    acquisition: optional
    space: optional

electrodes:
  suffixes:
  - electrodes
  extensions:
  - .json
  - .tsv
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    acquisition: optional
    space: optional

events:
  suffixes:
  - events
  extensions:
  - .json
  - .tsv
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional

photo:
  suffixes:
  - photo
  extensions:
  - .jpg
  - .png
  - .tif
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    acquisition: optional

timeseries:
  suffixes:
  - physio
  - stim
  extensions:
  - .tsv.gz
  - .json
  datatypes:
  - eeg
  entities:
    subject: required
    session: optional
    task: required
    acquisition: optional
    run: optional
    recording: optional

---
anat_nonparametric_common:
  $ref: rules.datatypes.raw.anat.nonparametric
  entities:
    $ref: rules.datatypes.raw.anat.nonparametric.entities
    space: optional
    description: optional

anat_parametric_common:
  $ref: rules.datatypes.raw.anat.parametric
  entities:
    $ref: rules.datatypes.raw.anat.parametric.entities
    space: optional
    description: optional

anat_defacemask_common:
  $ref: rules.datatypes.raw.anat.defacemask
  entities:
    $ref: rules.datatypes.raw.anat.defacemask.entities
    space: optional
    description: optional

anat_multiecho_common:
  $ref: rules.datatypes.raw.anat.multiecho
  entities:
    $ref: rules.datatypes.raw.anat.multiecho.entities
    space: optional
    description: optional

anat_multiflip_common:
  $ref: rules.datatypes.raw.anat.multiflip
  entities:
    $ref: rules.datatypes.raw.anat.multiflip.entities
    space: optional
    description: optional

anat_multiinversion_common:
  $ref: rules.datatypes.raw.anat.multiinversion
  entities:
    $ref: rules.datatypes.raw.anat.multiinversion.entities
    space: optional
    description: optional

anat_mp2rage_common:
  $ref: rules.datatypes.raw.anat.mp2rage
  entities:
    $ref: rules.datatypes.raw.anat.mp2rage.entities
    space: optional
    description: optional

anat_vfamt_common:
  $ref: rules.datatypes.raw.anat.vfamt
  entities:
    $ref: rules.datatypes.raw.anat.vfamt.entities
    space: optional
    description: optional

anat_mtr_common:
  $ref: rules.datatypes.raw.anat.mtr
  entities:
    $ref: rules.datatypes.raw.anat.mtr.entities
    space: optional
    description: optional

beh_timeseries_common:
  $ref: rules.datatypes.raw.beh.timeseries
  entities:
    $ref: rules.datatypes.raw.beh.timeseries.entities
    space: optional
    description: optional

beh_noncontinuous_common:
  $ref: rules.datatypes.raw.beh.noncontinuous
  entities:
    $ref: rules.datatypes.raw.beh.noncontinuous.entities
    space: optional
    description: optional

dwi_dwi_common:
  $ref: rules.datatypes.raw.dwi.dwi
  entities:
    $ref: rules.datatypes.raw.dwi.dwi.entities
    space: optional
    description: optional

dwi_sbref_common:
  $ref: rules.datatypes.raw.dwi.sbref
  entities:
    $ref: rules.datatypes.raw.dwi.sbref.entities
    space: optional
    description: optional

dwi_timeseries_common:
  $ref: rules.datatypes.raw.dwi.timeseries
  entities:
    $ref: rules.datatypes.raw.dwi.timeseries.entities
    space: optional
    description: optional

eeg_eeg_common:
  $ref: rules.datatypes.raw.eeg.eeg
  entities:
    $ref: rules.datatypes.raw.eeg.eeg.entities
    space: optional
    description: optional

eeg_channels_common:
  $ref: rules.datatypes.raw.eeg.channels
  entities:
    $ref: rules.datatypes.raw.eeg.channels.entities
    space: optional
    description: optional

eeg_coordsystem_common:
  $ref: rules.datatypes.raw.eeg.coordsystem
  entities:
    $ref: rules.datatypes.raw.eeg.coordsystem.entities
    space: optional
    description: optional

eeg_electrodes_common:
  $ref: rules.datatypes.raw.eeg.electrodes
  entities:
    $ref: rules.datatypes.raw.eeg.electrodes.entities
    space: optional
    description: optional

eeg_events_common:
  $ref: rules.datatypes.raw.eeg.events
  entities:
    $ref: rules.datatypes.raw.eeg.events.entities
    space: optional
    description: optional

eeg_photo_common:
  $ref: rules.datatypes.raw.eeg.photo
  entities:
    $ref: rules.datatypes.raw.eeg.photo.entities
    space: optional
    description: optional

eeg_timeseries_common:
  $ref: rules.datatypes.raw.eeg.timeseries
  entities:
    $ref: rules.datatypes.raw.eeg.timeseries.entities
    space: optional
    description: optional

fmap_fieldmaps_common:
  $ref: rules.datatypes.raw.fmap.fieldmaps
  entities:
    $ref: rules.datatypes.raw.fmap.fieldmaps.entities
    space: optional
    description: optional

fmap_pepolar_common:
  $ref: rules.datatypes.raw.fmap.pepolar
  entities:
    $ref: rules.datatypes.raw.fmap.pepolar.entities
    space: optional
    description: optional

fmap_TB1DAM_common:
  $ref: rules.datatypes.raw.fmap.TB1DAM
  entities:
    $ref: rules.datatypes.raw.fmap.TB1DAM.entities
    space: optional
    description: optional

fmap_TB1EPI_common:
  $ref: rules.datatypes.raw.fmap.TB1EPI
  entities:
    $ref: rules.datatypes.raw.fmap.TB1EPI.entities
    space: optional
    description: optional

fmap_RFFieldMaps_common:
  $ref: rules.datatypes.raw.fmap.RFFieldMaps
  entities:
    $ref: rules.datatypes.raw.fmap.RFFieldMaps.entities
    space: optional
    description: optional

fmap_TB1SRGE_common:
  $ref: rules.datatypes.raw.fmap.TB1SRGE
  entities:
    $ref: rules.datatypes.raw.fmap.TB1SRGE.entities
    space: optional
    description: optional

fmap_parametric_common:
  $ref: rules.datatypes.raw.fmap.parametric
  entities:
    $ref: rules.datatypes.raw.fmap.parametric.entities
    space: optional
    description: optional

func_func_common:
  $ref: rules.datatypes.raw.func.func
  entities:
    $ref: rules.datatypes.raw.func.func.entities
    space: optional
    description: optional

func_phase_common:
  $ref: rules.datatypes.raw.func.phase
  entities:
    $ref: rules.datatypes.raw.func.phase.entities
    space: optional
    description: optional

func_events_common:
  $ref: rules.datatypes.raw.func.events
  entities:
    $ref: rules.datatypes.raw.func.events.entities
    space: optional
    description: optional

func_timeseries_common:
  $ref: rules.datatypes.raw.func.timeseries
  entities:
    $ref: rules.datatypes.raw.func.timeseries.entities
    space: optional
    description: optional

ieeg_ieeg_common:
  $ref: rules.datatypes.raw.ieeg.ieeg
  entities:
    $ref: rules.datatypes.raw.ieeg.ieeg.entities
    space: optional
    description: optional

ieeg_channels_common:
  $ref: rules.datatypes.raw.ieeg.channels
  entities:
    $ref: rules.datatypes.raw.ieeg.channels.entities
    space: optional
    description: optional

ieeg_coordsystem_common:
  $ref: rules.datatypes.raw.ieeg.coordsystem
  entities:
    $ref: rules.datatypes.raw.ieeg.coordsystem.entities
    space: optional
    description: optional

ieeg_electrodes_common:
  $ref: rules.datatypes.raw.ieeg.electrodes
  entities:
    $ref: rules.datatypes.raw.ieeg.electrodes.entities
    space: optional
    description: optional

ieeg_events_common:
  $ref: rules.datatypes.raw.ieeg.events
  entities:
    $ref: rules.datatypes.raw.ieeg.events.entities
    space: optional
    description: optional

ieeg_photo_common:
  $ref: rules.datatypes.raw.ieeg.photo
  entities:
    $ref: rules.datatypes.raw.ieeg.photo.entities
    space: optional
    description: optional

ieeg_timeseries_common:
  $ref: rules.datatypes.raw.ieeg.timeseries
  entities:
    $ref: rules.datatypes.raw.ieeg.timeseries.entities
    space: optional
    description: optional

meg_meg_common:
  $ref: rules.datatypes.raw.meg.meg
  entities:
    $ref: rules.datatypes.raw.meg.meg.entities
    space: optional
    description: optional

meg_calibration_common:
  $ref: rules.datatypes.raw.meg.calibration
  entities:
    $ref: rules.datatypes.raw.meg.calibration.entities
    space: optional
    description: optional

meg_crosstalk_common:
  $ref: rules.datatypes.raw.meg.crosstalk
  entities:
    $ref: rules.datatypes.raw.meg.crosstalk.entities
    space: optional
    description: optional

meg_headshape_common:
  $ref: rules.datatypes.raw.meg.headshape
  entities:
    $ref: rules.datatypes.raw.meg.headshape.entities
    space: optional
    description: optional

meg_markers_common:
  $ref: rules.datatypes.raw.meg.markers
  entities:
    $ref: rules.datatypes.raw.meg.markers.entities
    space: optional
    description: optional

meg_coordsystem_common:
  $ref: rules.datatypes.raw.meg.coordsystem
  entities:
    $ref: rules.datatypes.raw.meg.coordsystem.entities
    space: optional
    description: optional

meg_channels_common:
  $ref: rules.datatypes.raw.meg.channels
  entities:
    $ref: rules.datatypes.raw.meg.channels.entities
    space: optional
    description: optional

meg_events_common:
  $ref: rules.datatypes.raw.meg.events
  entities:
    $ref: rules.datatypes.raw.meg.events.entities
    space: optional
    description: optional

meg_photo_common:
  $ref: rules.datatypes.raw.meg.photo
  entities:
    $ref: rules.datatypes.raw.meg.photo.entities
    space: optional
    description: optional

meg_timeseries_common:
  $ref: rules.datatypes.raw.meg.timeseries
  entities:
    $ref: rules.datatypes.raw.meg.timeseries.entities
    space: optional
    description: optional

micr_microscopy_common:
  $ref: rules.datatypes.raw.micr.microscopy
  entities:
    $ref: rules.datatypes.raw.micr.microscopy.entities
    space: optional
    description: optional

micr_photo_common:
  $ref: rules.datatypes.raw.micr.photo
  entities:
    $ref: rules.datatypes.raw.micr.photo.entities
    space: optional
    description: optional

perf_asl_common:
  $ref: rules.datatypes.raw.perf.asl
  entities:
    $ref: rules.datatypes.raw.perf.asl.entities
    space: optional
    description: optional

perf_aslcontext_common:
  $ref: rules.datatypes.raw.perf.aslcontext
  entities:
    $ref: rules.datatypes.raw.perf.aslcontext.entities
    space: optional
    description: optional

perf_asllabeling_common:
  $ref: rules.datatypes.raw.perf.asllabeling
  entities:
    $ref: rules.datatypes.raw.perf.asllabeling.entities
    space: optional
    description: optional

perf_timeseries_common:
  $ref: rules.datatypes.raw.perf.timeseries
  entities:
    $ref: rules.datatypes.raw.perf.timeseries.entities
    space: optional
    description: optional

pet_pet_common:
  $ref: rules.datatypes.raw.pet.pet
  entities:
    $ref: rules.datatypes.raw.pet.pet.entities
    space: optional
    description: optional

pet_blood_common:
  $ref: rules.datatypes.raw.pet.blood
  entities:
    $ref: rules.datatypes.raw.pet.blood.entities
    space: optional
    description: optional

pet_events_common:
  $ref: rules.datatypes.raw.pet.events
  entities:
    $ref: rules.datatypes.raw.pet.events.entities
    space: optional
    description: optional

pet_timeseries_common:
  $ref: rules.datatypes.raw.pet.timeseries
  entities:
    $ref: rules.datatypes.raw.pet.timeseries.entities
    space: optional
    description: optional

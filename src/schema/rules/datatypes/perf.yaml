---
asl:
  suffixes:
  - asl
  - m0scan
  extensions:
  - niigz
  - nii
  - json
  datatypes:
  - perf
  entities:
    subject: required
    session: optional
    acquisition: optional
    reconstruction: optional
    direction: optional
    run: optional

aslcontext:
  suffixes:
  - aslcontext
  extensions:
  - tsv
  - json
  datatypes:
  - perf
  entities:
    subject: required
    session: optional
    acquisition: optional
    reconstruction: optional
    direction: optional
    run: optional

asllabeling:
  suffixes:
  - asllabeling
  extensions:
  - jpg
  datatypes:
  - perf
  entities:
    subject: required
    session: optional
    acquisition: optional
    reconstruction: optional
    run: optional

timeseries:
  suffixes:
  - physio
  - stim
  extensions:
  - tsvgz
  - json
  datatypes:
  - perf
  entities:
    subject: required
    session: optional
    acquisition: optional
    reconstruction: optional
    direction: optional
    run: optional
    recording: optional

---
CommonProvenanceFields:
  selectors:
    - "true" # rule is validated for all sidecars files
  fields:
    FileGeneratedById: optional
    SidecarGeneratedBy: optional
    FileDigest: optional
    FileType: optional

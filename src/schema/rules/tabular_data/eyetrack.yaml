---
EyeTracking:
  selectors:
    - suffix == "eyetrack"
  extension:
    - '.tsv.gz'
  columns:
    x_coordinate: required
    y_coordinate: required
    et_timestamp: recommended
    pupil_size: optional
  initial_columns:
    - x_coordinate
    - y_coordinate
  additional_columns: allowed_if_defined

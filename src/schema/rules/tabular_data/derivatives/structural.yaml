---
morphometric:
  selectors:
  - dataset.datasettype == "derivative"
  - suffix == "morph"
  columns:
    index: RECOMMENDED
    structure_name: RECOMMENDED
    centroid: OPTIONAL
    surface_volume: OPTIONAL
    intensity: OPTIONAL
    thickness: OPTIONAL
    area: OPTIONAL
    curvature: OPTIONAL
  additional_columns: allowed

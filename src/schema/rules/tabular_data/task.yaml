---
TaskEvents:
    selectors:
    - entities contains "task"
    - suffix == "events"
    columns:
        onset: REQUIRED
        duration: REQUIRED
        sample: OPTIONAL
        trial_type: OPTIONAL
        response_time: OPTIONAL
        value: OPTIONAL
        HED: OPTIONAL
        stim_file: OPTIONAL
    additional_columns: allowed

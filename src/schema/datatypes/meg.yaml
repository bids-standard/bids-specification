---
# First group
- suffixes:
    - meg
  extensions:
    - /  # corresponds to BTi/4D data
    - .ds/
    - .json
    - .fif
    - .sqd
    - .con
    - .raw
    - .ave
    - .mrk
    - .kdf
    - .mhd
  entities:
    sub: required
    ses: optional
    task: required
    acq: optional
    run: optional
    proc: optional
    split: optional

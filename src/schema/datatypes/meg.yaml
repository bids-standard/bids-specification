- suffixes:
    - meg
  extensions:
    - 4d/
    - ctf/
    - .json
    - .fif
    - .sqd
    - .con
    - .raw
    - .ave
    - .mrk
    - .kdf
    - .mhd
  entities:
    sub: required
    ses: optional
    task: required
    acq: optional
    run: optional
    proc: optional
- suffixes:
    - headshape
  extensions:
    - .pos
    - .txt
  entities:
    sub: required
    ses: optional
    acq: optional
    space: optional
- suffixes:
    - markers
  extensions:
    - .json
  entities:
    sub: required
    ses: optional
    task: optional
    acq: optional
    space: optional

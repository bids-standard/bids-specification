---
- datatypes:
    - meg
    - eeg
    - ieeg
    - func
    - beh
  suffixes:
    - physio
  extensions:
    - .json
    - .tsv.gz
  entities:
    - matches: required
    - sub: required
      ses: optional
      task: required
      run: optional
